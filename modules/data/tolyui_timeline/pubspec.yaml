# 本文件定义 tolyui_timeline 包及其 workspace 解析边界。

name: tolyui_timeline
description: "tolyui timeline for tolyui"
version: 0.0.1
resolution: workspace
homepage: https://github.com/TolyFx/toly_ui
repository: https://github.com/TolyFx/toly_ui/tree/main/modules/data/tolyui_timeline

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
