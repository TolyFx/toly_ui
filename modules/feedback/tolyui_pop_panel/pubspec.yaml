# 本文件定义 tolyui_pop_panel 包及其 workspace 解析边界。

name: tolyui_pop_panel
description: A reusable Flutter bottom panel scaffold with a consistent header, content area, and optional footer.
version: 0.0.1+1
resolution: workspace
homepage: https://github.com/TolyFx/toly_ui
repository: https://github.com/TolyFx/toly_ui/tree/main/modules/feedback/tolyui_pop_panel

environment:
  sdk: ^3.6.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
