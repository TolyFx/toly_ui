# 本文件定义 tolyui_rich_input 包及其 workspace 解析边界。

name: tolyui_rich_input
description: A rich text input widget for Flutter with support for images, emojis, and files.
version: 1.0.0+1
resolution: workspace
homepage: https://github.com/TolyFx/toly_ui
repository: https://github.com/TolyFx/toly_ui/tree/main/modules/form/tolyui_rich_input

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  characters: ^1.3.0
  flutter_quill: ^11.4.1
  url_launcher: ^6.2.0
  quill_native_bridge: ^11.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
