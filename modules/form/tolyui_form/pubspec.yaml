# 本文件定义 tolyui_form 包及其 workspace 解析边界。

name: tolyui_form
description: Compact, composable form controls for Toly UI.
version: 0.0.1+1
resolution: workspace
homepage: https://github.com/TolyFx/toly_ui
repository: https://github.com/TolyFx/toly_ui/tree/main/modules/form/tolyui_form

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
