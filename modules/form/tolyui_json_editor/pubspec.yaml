# 本文件定义 tolyui_json_editor 包及其 workspace 解析边界。

name: tolyui_json_editor
description: "JSON editor with syntax highlighting, validation and formatting for tolyui"
version: 0.0.1+1
resolution: workspace
homepage: https://github.com/TolyFx/toly_ui
repository: https://github.com/TolyFx/toly_ui/tree/main/modules/form/tolyui_json_editor

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
