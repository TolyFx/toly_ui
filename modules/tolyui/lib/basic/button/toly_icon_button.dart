import 'package:flutter/material.dart';

class TolyIconButton extends StatelessWidget {
  const TolyIconButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
