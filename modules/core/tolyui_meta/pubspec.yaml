# 本文件定义 tolyui_meta 包及其 workspace 解析边界。

name: tolyui_meta
description: "pure data for tolyui."
version: 0.0.2+2
resolution: workspace
homepage: http://toly1994.com/ui
repository: https://github.com/TolyFx/toly_ui/tree/main/modules/tolyui_meta

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
